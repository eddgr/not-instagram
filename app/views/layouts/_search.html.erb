<%= form_tag(search_path, method: :get, class: "form-inline mr-auto d-none d-lg-block") do %>
  <%= text_field_tag(:search, params[:search], class: "form-control form-control-sm text-center", placeholder: "Search Posts") %>
<% end %>
