<%= form_tag(search_path, method: :get, class: "form-inline mr-auto") do %>
  <%= text_field_tag(:search, params[:search], class: "form-control mr-sm-2", placeholder: "Search posts") %>
  <%= submit_tag ("Search"), class: "btn btn-outline-dark my-2 my-sm-0" %>
<% end %>
